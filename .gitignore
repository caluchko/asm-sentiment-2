# Python
.venv/
__pycache__/
*.pyc

# Secrets — never commit
.env
*.key
service-account*.json
*-credentials.json

# OS
.DS_Store

# Local Claude Code settings (personal, machine-specific)
.claude/settings.local.json
CLAUDE.md

# Large / regenerable data — excluded by default
data/raw/
data/validation/
*.parquet
*.csv

# ...but DO commit the small files the dashboard reads at runtime
!data/processed/articles_enriched.parquet
!data/classified/sample.parquet
